<%= simple_form_for([@message,@message.comments.build]) do |f| %>
  <%= f.input :content, label: "Comment", error: "All comments require text." %>
  <%= f.button :submit, class: "btn-default btn-custom bot-space" %>
<% end %>