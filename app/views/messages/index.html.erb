<% @messages.each do |message| %>
    <div class="col-md-4">
        <div class="message">
            <h2 class="message-title"><%= message.title %></h3>
            <%= link_to("View Post", message_path(message), class: "btn-custom") %>
        </div>
    </div>
<% end %>