<h1>Hello!</h1>

<ul>
    <% @messages.each do |message| %>
        <h3><%= message.title %></h3>
        <%= link_to("View Post", message_path(message), class: "btn btn-default") %>
    <% end %>
</ul>

<%= link_to("New Post", new_message_path) %>