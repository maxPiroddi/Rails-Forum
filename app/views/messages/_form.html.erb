<%= simple_form_for @message do |f| %>
  <%= f.input :title, label: "Title", error: "All posts require a title!" %>
  <%= f.input :description, label: "Content", error: "Please ensure you have entered text." %>
  <%= f.button :submit, class: "btn-default" %>
<% end %>